{
  "$id": "http://schema.openbikebox.de/locations.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "locations",
  "description": "this is the request to get locations.",
  "type": "object",
  "required": [],
  "properties": {
    "format": {
      "type": "string",
      "enum": ["list", "geojson"],
      "default": "list"
    }
  }
}
